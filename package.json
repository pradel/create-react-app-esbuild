{
  "private": true,
  "scripts": {
    "release": "standard-version -a && yarn workspace craco-esbuild npm publish"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "standard-version": "9.1.0"
  }
}
